-- Create the database only if it doesn't exist
CREATE DATABASE IF NOT EXISTS alx_book_store;

-- Optional: switch to the new database
USE alx_book_store;

-- Feedback message (MySQL doesn’t print custom messages directly in SQL,
-- but you’ll see warnings if the DB already exists)
